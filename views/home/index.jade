extends ../layout.jade

block content
    div(id='image-background')
        div(id='form-container')
            form(action='/room', method='POST', class='paper-1', id='form-home-login', name='form-home-login')
                section
                    h1 This is Hoot.
                div(id='fab-home-login')
                section
                    label(for='home-login-username') Enter your username to start.
                    input(id='home-login-username', name='username' type='text', placeholder='Username', autocomplete='off', value='')

    script(src='/javascripts/index.js')